<%#= debug %>
<%= semantic_form_for :new_playlist, :url => {:action => "create_playlist", :controller => "admin/songs"} do |f| %>
    <%= f.input :name, :label => "Playlist name", :class => "primary" %>
    <%= f.input :songs, :as => :check_boxes, :collection => Song.order("id ASC").all %>
   	<%= f.actions do %>
   		<%= f.action :submit, :button_html => {:value => "Create Playlist", :class => "primary"}  %>
   		
        <%#=	f.action :submit,  :button_html => {:value => "Cancel", :class => "primary" }%>
    <% end %>
<% end %>
<%= link_to "Back",  admin_playlist_path%>